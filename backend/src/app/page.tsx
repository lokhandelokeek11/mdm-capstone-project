export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Customer Journey Intelligence API</h1>
      <p>Version 0.1.0 — API server is running.</p>
      <p>API base: <code>/api</code></p>
    </main>
  );
}
